# Secret - stores sensitive data (base64 encoded)
# NEVER commit real secrets to version control
# Use tools like Sealed Secrets, External Secrets, or AWS Secrets Manager in production
# To generate base64 values: echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secrets
  namespace: taskflow
type: Opaque
data:
  # Replace these with actual base64-encoded values
  # echo -n "taskflow_admin" | base64
  DB_USERNAME: dGFza2Zsb3dfYWRtaW4=
  # echo -n "your-secure-password" | base64
  DB_PASSWORD: eW91ci1zZWN1cmUtcGFzc3dvcmQ=
